// if this = that and whatever <= this:
// if (this == that) && (whatever <= this) {

7 == "7";

var numbers = [0, 1, 2, 3, 4];

numbers.forEach(number => {});

function logThing(argument) {
  console.log(argument);
}

function hello(name) {
  var output = "Hello " + name;
  logThing(output);
  return output;
}

hello("josh");
